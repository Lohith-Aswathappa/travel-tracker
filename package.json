{
  "name": "adventure-atlas-apple",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "dev": "npm run build && npx serve dist -l 4321"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
